{
  "name": "medical-equipment-tracker",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/catarizea/medical-equipment-tracker.git",
  "author": "Catalin Rizea <catalinrizea1@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "authentication",
    "hasura",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "build": "cross-env NODE_ENV=production yarn --cwd frontend build",
    "clear": "rm -rf node_modules */node_modules packages/*/node_modules",
    "containers-stop": "docker stop medical-equipment-tracker_postgres_1 medical-equipment-tracker_graphql-engine_1",
    "hasura-start:dev": "cross-env NODE_ENV=development dotenv -e .env.development.local -- docker-compose up -d",
    "hasura-start": "cross-env NODE_ENV=production dotenv -e .env.production.local -- docker-compose up -d",
    "start": "cross-env NODE_ENV=production yarn --cwd authentication start"
  },
  "dependencies": {
    "cross-env": "7.0.2"
  }
}
