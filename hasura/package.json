{
  "name": "@medical-equipment-tracker/hasura",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "console": "hasura console",
    "metadata": "hasura metadata",
    "migrate": "hasura migrate",
    "squash:init": "hasura migrate create 'init' --from-server",
    "squash:apply": "hasura migrate apply --version '1596622553868' --skip-execution",
    "squash:status": "hasura migrate status"
  },
  "dependencies": {
    "dotenv-cli": "4.0.0"
  },
  "devDependencies": {
    "hasura-cli": "2.0.9"
  }
}
