{
  "name": "@medical-equipment-tracker/hasura",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "console": "hasura console",
    "migrate": "hasura migrate",
    "squash:init": "hasura migrate create 'init' --from-server",
    "squash:apply": "hasura migrate apply --version '1596032087022' --skip-execution",
    "squash:status": "hasura migrate status"
  },
  "dependencies": {
    "dotenv-cli": "3.2.0"
  },
  "devDependencies": {
    "hasura-cli": "1.3.0"
  }
}
