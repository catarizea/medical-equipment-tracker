import { defineMessages } from 'react-intl';

const messages = defineMessages({
  cannotUse: {
    id: 'CodeReader.cannotUse',
    defaultMessage: 'Cannot use webcam to read codes...',
  },
});

export default messages;
